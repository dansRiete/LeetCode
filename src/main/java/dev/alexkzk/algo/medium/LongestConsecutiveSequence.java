package dev.alexkzk.algo.medium;

public class LongestConsecutiveSequence {

    /**
     * Returns the length of the longest consecutive elements sequence in an unsorted array.
     *
     * <p><b>Problem (LeetCode 128 – Medium):</b> Given an unsorted array of integers {@code nums},
     * return the length of the longest sequence of consecutive integers.
     * You must write an algorithm that runs in <b>O(n)</b> time.
     *
     * <p><b>Examples:</b>
     * <pre>
     *   [100, 4, 200, 1, 3, 2]        →  4   (sequence: 1, 2, 3, 4)
     *   [0, 3, 7, 2, 5, 8, 4, 6, 0, 1] →  9   (sequence: 0, 1, 2, 3, 4, 5, 6, 7, 8)
     *   []                             →  0
     *   [1]                            →  1
     * </pre>
     *
     * <p><b>Constraints:</b>
     * <ul>
     *   <li>{@code 0 <= nums.length <= 10^5}</li>
     *   <li>{@code -10^9 <= nums[i] <= 10^9}</li>
     * </ul>
     *
     * <p><b>Hint:</b> Think about when it makes sense to start counting a sequence.
     * A number {@code n} is only the <em>start</em> of a sequence if {@code n-1} is not present.
     *
     * @param nums unsorted array of integers (may contain duplicates)
     * @return length of the longest consecutive sequence
     */
    public static int longestConsecutive(int[] nums) {
        throw new UnsupportedOperationException("Not implemented yet");
    }
}
